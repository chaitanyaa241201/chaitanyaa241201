apiVersion: v1
kind: Service
metadata: 
  name: my-service
spec:
    type: NodePort
    selector: 
       name: chaitanya
    ports:
        - name: tomcat 
          port: 8080
          targetPort: 8080
